<h1>Tagged Notes</h1>

<% @notes.each do |r| %>
  <%= markdown(r.title) %>
  <% r.tag_list.each do |tag| %>
    <%= link_to tag, tagged_path(tag: tag) %>
  <% end %></p>
<% end %>

<%= link_to 'All Notes', notes_path %>
